services:
  desktop:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      VNC_RESOLUTION: "1280x720"
      VNC_DEPTH: "24"
    shm_size: 256m
